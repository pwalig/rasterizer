cmake_minimum_required(VERSION 3.16)
project(rast)
add_library(rast INTERFACE)
target_include_directories(rast INTERFACE include ext/glm ext/stb)
target_compile_definitions(rast INTERFACE GLM_ENABLE_EXPERIMENTAL)
target_compile_features(rast INTERFACE cxx_std_20)